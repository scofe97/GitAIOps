# 5.3 Blue/Green 배포 — 예상 결과

## 상태 확인

```
$ kubectl argo rollouts get rollout notiflex-api -n notiflex
Name:            notiflex-api
Namespace:       notiflex
Status:          ✔ Healthy
Strategy:        BlueGreen
  StableRS:      notiflex-api-xxxxxxxxxx
  ActiveRS:      notiflex-api-xxxxxxxxxx
Images:          asia-northeast3-docker.pkg.dev/.../api:v0.2.0 (stable, active)
```

## 체크리스트

- [ ] Argo Rollouts 설치됨
- [ ] Deployment → Rollout 전환 완료
- [ ] deployment.yaml 삭제됨 (rollout.yaml만 존재)
- [ ] preview 서비스 생성됨
- [ ] Blue/Green 배포 동�� 확인 (30초 후 auto-promote)
- [ ] /update-docs 실행 안내됨

## 이 단계 이후 아키텍처

```
GKE: notiflex-cluster
├── argo-rollouts namespace
│   └── Argo Rollouts Controller
├── notiflex namespace
│   ├── Rollout: notiflex-api (Blue/Green, v0.2.0)
│   ├��─ Service: notiflex-api (active)
│   ├── Service: notiflex-api-preview (preview)
│   ├── Gateway + HTTPRoute
│   └── HealthCheckPolicy
├── monitoring namespace
└── argocd namespace

배포 전략: Rolling → Blue/Green (진화)
```
