# 8.2 분산 트레이싱: Tempo — 예상 결과

## 상태 확인

```
$ kubectl get pods -n monitoring -l app.kubernetes.io/name=tempo
NAME                     READY   STATUS    RESTARTS   AGE
tempo-xxxxxxxxxx-xxxxx   1/1     Running   0          2m

Grafana → Explore → Tempo → Search
(notiflex-api 서비스의 트레이스 조회 가능)
```

## 체크리스트

- [ ] Tempo 설치됨 (OTLP gRPC receiver 활성화)
- [ ] Notiflex에 OpenTelemetry SDK 추가됨
- [ ] Dockerfile: golang:1.25-alpine 사용
- [ ] OTEL_EXPORTER_OTLP_ENDPOINT 환경변수 설정됨
- [ ] Grafana에서 트레이스 조회 가능
- [ ] /id, /health 요청에 대한 span 확인
- [ ] JOURNEY.md `현재 버전` — OTel SDK 버전 기재 (`grep opentelemetry app/go.mod`로 조회)

## 이 단계 이후 아키텍처

```
GKE: notiflex-cluster
├── monitoring namespace
│   ├── Prometheus + Grafana + Alertmanager
│   ├── Loki + Fluent Bit
│   └── Tempo (분산 트레이싱)
├── kafka namespace
│   └── Kafka (KRaft)
├── notiflex namespace
│   ├── Rollout: notiflex-api (v0.4.0)
│   │   └── env: VALKEY_ADDR, KAFKA_BROKER, OTEL_EXPORTER_OTLP_ENDPOINT
│   ├── Valkey
│   └── Gateway
...

관측성: 메트릭(Prometheus) + 로깅(Loki) + 트레이싱(Tempo)
```
